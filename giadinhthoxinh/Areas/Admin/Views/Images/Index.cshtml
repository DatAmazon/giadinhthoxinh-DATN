@model IEnumerable<giadinhthoxinh.Models.tblImage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblProduct.sProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblProduct.sProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_iImageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_iImageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_iImageID })
        </td>
    </tr>
}

</table>
