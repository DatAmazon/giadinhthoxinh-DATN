@model IEnumerable<giadinhthoxinh.Models.tblMaterColor>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sMaterColor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblMaterial.sMaterialName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sMaterColor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblMaterial.sMaterialName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_iMaterColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_iMaterColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_iMaterColorID })
        </td>
    </tr>
}

</table>
